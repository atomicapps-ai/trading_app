"""Trading agents package.

Every agent class is constructed with a Settings instance and is pure w.r.t.
its inputs. No wall-clock calls, no direct broker or data-service imports
outside of the interfaces already wired through services/.
"""
